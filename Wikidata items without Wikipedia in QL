/*
This has been generated by the overpass-turbo wizard.
The original search was:
“type:relation and boundary=administrative and admin_level=9 in "TW”
*/
[out:xml]/*fixed by auto repair*/[timeout:250];
// fetch area “TW” to search in
{{geocodeArea:TW}}->.searchArea;
// gather results
(
  // query part for: “boundary=administrative and admin_level=9”
  node["wikipedia"!~".*"]["wikidata"](area.searchArea);  
  way["wikipedia"!~".*"]["wikidata"](area.searchArea);  
  relation["wikipedia"!~".*"]["wikidata"](area.searchArea);
);
// print results
out meta;
>;
out meta qt;
