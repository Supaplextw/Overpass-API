/*
This has been generated by the overpass-turbo wizard.
The original search was:
“amenity=school and "addr:housenumber" is null”
*/
[out:xml]/*fixed by auto repair*/[timeout:250];
// fetch area “新店區” to search in
{{geocodeArea:TW}}->.searchArea;
// gather results
(
  // query part for: “amenity=school and "addr:housenumber"!=*”
  node["amenity"="school"]["addr:housenumber"!~".*"](area.searchArea);
  way["amenity"="school"]["addr:housenumber"!~".*"](area.searchArea);
  relation["amenity"="school"]["addr:housenumber"!~".*"](area.searchArea);
);
// print results
out meta;/*fixed by auto repair*/
>;
out meta qt;/*fixed by auto repair*/
