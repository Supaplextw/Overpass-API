/*
This has been generated by the overpass-turbo wizard.
The original search was:
type:node and emergency=assembly_point and without addr:housenumber in BBox
*/

[out:xml]/*fixed by auto repair*/[timeout:225];

// gather results
(
  // query part for: “building=yes and emergency=assembly_point and amenity!=*”
  node["name"]["addr:street"]["addr:housenumber"!~".*"]({{bbox}});
);
// print results
out meta;/*fixed by auto repair*/
>;
out meta qt;/*fixed by auto repair*/
//用 MapCSS 顯示名稱
{{style:
node{
  text: eval('tag("name")');
}
}}
